# 青鹭 QCOS 门店部署与更新说明

把在家里开发电脑上做好的 QCOS 系统，搬到门店那台"常开电脑"上运行；以及后续版本更新怎么操作。

---

## 一、你要搬运什么

`qcos_部署包.zip` / `qcos_更新包.zip` 都是**纯代码包（不含 `qcos.db`）**，重点：

- `app.py` / `analytics.py` / `models.py` / `config.py` 等所有 `.py`
- `templates/` 前端页面、`static/` 静态资源
- `requirements.txt` —— 依赖清单（flask + openpyxl）
- `启动.bat` / `注册开机自启.bat` —— 一键启动与开机自启
- `.env.example` —— 安全配置模板（真实 `.env` 由 `启动.bat` 首次运行时自动生成，不进 Git）
- `AI_ANALYSIS_PROMPT.md` —— AI 分析提示词

> **关于数据库 `qcos.db`**：本包**不含**它。门店首次启动会自动建一个干净的空库（含 `admin` 初始账号，密码在首次启动时由控制台输出或 `.env` 文件中 `QCOS_ADMIN_PASSWORD` 指定）。你门店自己的生意数据（`qcos.db`）在**本地生成、本地保留**，**更新时切勿用包去覆盖它**。

> 无需搬运：虚拟环境（venv 不在本目录内）、`__pycache__`、`*.log`、示例导出等缓存文件。`启动.bat` 会在门店电脑**首次运行时自动重建 venv 并装依赖**。

---

## 二、门店电脑前置条件

| 条件 | 说明 |
|---|---|
| 系统 | Windows 10 / 11（扫码盒为 Windows USB HID 免驱，插上即用） |
| Python | 需安装 **Python 3.11+**，安装时**务必勾选 "Add python.exe to PATH"** |
| 联网 | 仅**首次**启动需要联网装依赖；之后可离线运行 |
| 扫码盒 | 逊镭扫码盒 USB 即插即用，插到门店电脑任意 USB 口即可，系统自动识别键盘输入 |

---

## 三、首次部署（约 3 分钟）

1. **放文件**：把 `qcos_部署包.zip` 解压到门店电脑固定位置，例如 `D:\qcos\`。
2. **双击 `启动.bat`**：
   - 首次会自动创建 `venv` 并 `pip install` 依赖（联网，1–2 分钟）。
   - 之后 `init_db()` 自动建空库（含 admin 账号），启动 Flask，看到 `青鹭 QCOS 正在启动...` 即成功。
3. **浏览器登录**：打开 `http://localhost:5000` → 管理员 `admin`，密码见首次启动控制台输出或 `.env` 文件中的 `QCOS_ADMIN_PASSWORD`。

> 首屏若空白或报错，先看 `启动.bat` 黑窗口里的红色报错信息。

---

## 四、系统更新流程（版本迭代时用）

开发端发布新版本后，你会拿到 `qcos_更新包.zip`。更新**只换代码、不动数据**：

1. **先备份（必做）**：复制门店 `qcos/qcos.db` 到 U 盘 / 云盘，防万一。
2. **覆盖代码**：把 `qcos_更新包.zip` 解压，将其中的文件**覆盖**到门店 `qcos/` 目录。
   - 更新包**不含 `qcos.db`**，所以你的玩家档案、组局记录、会员余额一个都不会被碰。
   - 解压若提示"是否覆盖"，选"全部覆盖 / 替换"即可。
3. **重启系统**：
   - 手动跑 `启动.bat`：关掉黑窗口（Ctrl+C），重新双击 `启动.bat`。
   - 已注册开机自启：任务计划程序里右键 `QCOS_AutoStart` → "运行"，或直接重启门店电脑。
4. 浏览器开 `http://localhost:5000` 验证正常。

> **为什么放心覆盖**：`init_db()` 是幂等设计（全部 `CREATE TABLE IF NOT EXISTS`）。更新后首次启动只会**补建新增的表**，不删不改任何现有数据。
> **例外**：若某版本涉及**给旧表加新列**，开发端会另附一段迁移命令，请先按附件执行再重启。

---

## 五、多端访问

| 设备 | 访问地址 |
|---|---|
| 门店电脑本机 | `http://localhost:5000` |
| 同 WiFi 手机 / 平板 / 另一台电脑 | `http://<门店电脑局域网IP>:5000`（IP 用 `ipconfig` 查 IPv4，形如 `192.168.x.x`）|

- **手机/平板能看数据、进分析中心、导出报表，但不能用扫码盒收款**（扫码盒硬件接在门店电脑上）。
- 若其他设备打不开，多半是 **Windows 防火墙拦截了 5000 端口**，需在防火墙高级设置里放行"入站端口 5000/TCP"。

---

## 六、开机自启（推荐）

门店电脑常开，建议注册开机自启，断电恢复后无需人工干预：

1. **右键 `注册开机自启.bat` → 以管理员身份运行**。
2. 提示"已注册"即可。
3. 以后门店电脑开机即自动启动 QCOS，崩溃也会自动重启。

> 取消自启：`schtasks /Delete /TN "QCOS_AutoStart" /F`

---

## 七、安全与稳定建议（重要）

1. **关闭 debug 模式**：`app.py` 末行是 `app.run(host='0.0.0.0', port=5000, debug=True)`。
   长期在门店多端访问时，建议把 `debug=True` 改成 `debug=False`（避免调试器被远程利用、且改代码不再热重载）。开发电脑测试时保持 `True` 即可。
2. **定期备份数据库**：`qcos.db` 是唯一数据源。建议每周复制一份到 U 盘 / 云盘，防硬件损坏（更新前也务必备份一次）。
3. **固定门店电脑 IP**：在路由器给门店电脑分配**固定局域网 IP**（或 Windows 里设静态 IP），否则重启后 IP 变化，手机端地址要跟着改。

---

## 八、常见问题

- **连不上 `localhost:5000`**：确认 `启动.bat` 黑窗口还开着、没报错退出；任务栏应有 Python 进程。
- **其他设备连不上**：检查是否同一 WiFi / 同一网段；放行防火墙 5000 端口；确认 IP 正确。
- **扫码盒收款没反应**：确认扫码盒插在**门店电脑**的 USB 口（不是手机）；收银结账弹窗有"扫码确认区"，扫完出现回车即识别。
- **忘记管理员密码**：查看 `.env` 文件中的 `QCOS_ADMIN_PASSWORD`（首次部署时生成）。若已改密且遗忘，需直接改 `qcos.db` 的 `users` 表密码字段（联系开发者）。
- **首次启动卡在装依赖**：确认门店电脑能联网；或先在能联网的机器装好 venv 再整体拷贝（venv 含绝对路径，跨机通常可用，但 Python 小版本需一致）。
- **更新后页面没变化**：清浏览器缓存（Ctrl+F5 强刷）；确认更新包已覆盖到正确目录、并已重启。

---

## 九、版本说明

本包对应 **QCOS V1.5**（数据分析中心 + AI 经营报告导出系统），含：
收银 / 计费 / 会员 / CRM 玩家档案 / 运营大脑 / 智能组局 / 桌局反馈 / 分析中心（/analytics）。
所有功能均为只读消费现有数据，无数据库结构变更。

打包分两种：
- `qcos_部署包.zip` —— 首次部署用（纯代码，首次启动自动建空库）。
- `qcos_更新包.zip` —— 版本更新用（纯代码，覆盖即更新，保留门店 `qcos.db`）。
